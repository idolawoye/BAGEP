IDS, = glob_wildcards("{id}.json")

rule all:
	input:
		expand(["tsv_out/{id}.tsv"], id=IDS)

rule mykrobe:
	input:
		["mykrobe_out/{id}.json"]
	output:
		"tsv_out/{id}.tsv"
	shell:
		"python3 mykrobe/json_to_tsv.py {input} > {output}"
